{
  "name": "freepieces",
  "private": false,
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "check": "tsc --noEmit && tsc --noEmit --project tsconfig.client.json",
    "worker:dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260405.0",
    "@types/node": "^25.6.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.1",
    "wrangler": "^4.11.1"
  }
}
